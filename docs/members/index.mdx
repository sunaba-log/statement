---
title: メンバー紹介
description: Sunabalogチームのメンバーをご紹介します。
---

import MemberCard from "@site/src/components/MemberCard";
import MemberSimpleCard from "@site/src/components/MemberSimpleCard";
import { members, getAllDepartments } from "@site/src/data/members";
import ShellViewer from "@site/src/components/ShellViewer";
import GhostViewer from "@site/src/components/GhostViewer";

# メンバー紹介

Sunabalogチームの優秀なメンバーたちをご紹介します。私たちはユーザー第一のマインドセットで、最新の技術を活用しながら、高品質なプロダクトを創造しています。

## チーム構成

<div style={{ marginBottom: '2rem' }}>

{getAllDepartments().map((department) => {
const deptMembers = members.filter(m => m.department === department);
return (
<div key={department} style={{ marginBottom: '3rem' }}>
<h3 style={{
        borderBottom: '3px solid var(--ifm-color-primary)',
        paddingBottom: '0.5rem',
        marginBottom: '2rem'
      }}>
{department}
</h3>
<div style={{
        // display: 'grid',
        // gridTemplateColumns: 'repeat(auto-fit, minmax(320px, 1fr))',
        // gap: '2rem'
      }}>
     
{deptMembers.map((member) => (
  <>
<MemberSimpleCard
            key={member.id}
            name={member.name}
            role={member.role}
            department={member.department}
            joinDate={member.joinDate}
            bio={member.bio}
            image={member.image}
            skills={member.skills}
            specialty={member.specialty}
            hobbies={member.hobbies}
            github={member.github}
            twitter={member.twitter}
            website={member.website}
          /> 
          <ShellViewer
        title={`${department}の活動`}
        content={`このセクションでは、${department}のメンバーがどのようなプロジェクトに取り組んでいるかを紹介します。最新の技術スタックやプロジェクトの概要を通じて、チームの活躍をご覧ください。`}
      />
      <GhostViewer
        title={`${department}の雰囲気`}
        content={`${department}は、協力的で活発なチーム文化を持っています。定期的なミーティングやワークショップを通じて、メンバー同士が知識を共有し、成長しています。`}
      /></>
))}
</div>
</div>
);
})}

</div>

## チームの特徴

- 🎯 **品質重視** - コードレビューやテスト自動化を重視しています
- 🚀 **最新技術** - 新しい技術の導入と学習に積極的です
- 🤝 **チーム重視** - ドキュメント作成とナレッジ共有を大切にしています
- 📚 **継続的成長** - 定期的な技術研修とスキルアップの機会を提供しています

## メンバー統計

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
  gap: '1.5rem',
  marginTop: '2rem',
  marginBottom: '2rem'
}}>
  <div style={{
    padding: '1.5rem',
    background: 'var(--ifm-color-primary-lightest)',
    borderRadius: '8px',
    textAlign: 'center'
  }}>
    <div style={{
      fontSize: '2rem',
      fontWeight: 'bold',
      color: 'var(--ifm-color-primary)'
    }}>
      {members.length}
    </div>
    <div style={{
      fontSize: '0.9rem',
      color: 'var(--ifm-color-gray-700)',
      marginTop: '0.5rem'
    }}>
      チームメンバー
    </div>
  </div>
  
  <div style={{
    padding: '1.5rem',
    background: 'var(--ifm-color-secondary-lightest)',
    borderRadius: '8px',
    textAlign: 'center'
  }}>
    <div style={{
      fontSize: '2rem',
      fontWeight: 'bold',
      color: 'var(--ifm-color-secondary)'
    }}>
      {getAllDepartments().length}
    </div>
    <div style={{
      fontSize: '0.9rem',
      color: 'var(--ifm-color-gray-700)',
      marginTop: '0.5rem'
    }}>
      部門数
    </div>
  </div>

  <div style={{
    padding: '1.5rem',
    background: 'var(--ifm-color-success-lightest)',
    borderRadius: '8px',
    textAlign: 'center'
  }}>
    <div style={{
      fontSize: '2rem',
      fontWeight: 'bold',
      color: 'var(--ifm-color-success)'
    }}>
      {new Set(members.flatMap(m => m.skills)).size}
    </div>
    <div style={{
      fontSize: '0.9rem',
      color: 'var(--ifm-color-gray-700)',
      marginTop: '0.5rem'
    }}>
      技術スタック
    </div>
  </div>
</div>

## 技術スタック

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))',
  gap: '1.5rem',
  marginTop: '2rem',
  marginBottom: '2rem'
}}>
  <div style={{
    padding: '1.5rem',
    background: 'var(--ifm-color-gray-100)',
    borderRadius: '8px'
  }}>
    <h4 style={{ marginTop: 0 }}>言語・フレームワーク</h4>
    <ul style={{ margin: 0, paddingLeft: '1.5rem' }}>
      <li>Go / Node.js (TypeScript)</li>
      <li>Python / Java</li>
      <li>React / Next.js</li>
      <li>Vue.js</li>
    </ul>
  </div>

  <div style={{
    padding: '1.5rem',
    background: 'var(--ifm-color-gray-100)',
    borderRadius: '8px'
  }}>
    <h4 style={{ marginTop: 0 }}>インフラ・ツール</h4>
    <ul style={{ margin: 0, paddingLeft: '1.5rem' }}>
      <li>AWS (EC2, RDS, S3, Lambda)</li>
      <li>Docker / Kubernetes</li>
      <li>GitHub / GitHub Actions</li>
      <li>PostgreSQL / MongoDB</li>
    </ul>
  </div>
</div>

## メンバーを追加する方法

新しいメンバーを追加するには、以下のファイルを編集してください：

1. **`src/data/members.ts`** - メンバー情報を `members` 配列に追加
2. **メンバー画像** - `static/img/members/` フォルダに画像を配置
3. **自動反映** - このページが自動的に更新されます

### 追加例

```typescript
{
  id: "new-member",
  name: "新規 メンバー",
  nameEn: "New Member",
  role: "エンジニア",
  roleEn: "Engineer",
  department: "バックエンド チーム",
  joinDate: "2026-03-01",
  bio: "プロフィール説明...",
  image: "/img/members/new-member.jpg",
  skills: ["Skill1", "Skill2"],
  specialty: ["特技1", "特技2"],
  hobbies: ["趣味1", "趣味2"],
  github: "username",
  twitter: "username",
  website: "https://example.com",
}
```
